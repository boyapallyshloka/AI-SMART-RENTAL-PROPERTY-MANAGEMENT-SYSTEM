# Components

This directory houses reusable React components partitioned by feature and concern:
- `common/`: General shared components (Navbar, Footer, ErrorBoundary).
- `dashboard/`: Analytics, summary metrics, and chart widgets.
- `properties/`: Property cards, unit listings, filters, and media displays.
- `ui/`: Design system primitives (Buttons, Modals, Badges, Form Inputs).
